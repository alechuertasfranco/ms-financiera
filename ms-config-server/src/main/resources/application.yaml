spring:
  cloud:    config:      server:        git:
          uri: https://github.com/alechuertasfranco/ms-financiera
          search-paths: cofig-data
  application:    name: ms-config-server